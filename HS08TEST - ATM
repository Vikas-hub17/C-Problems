#include <bits/stdc++.h>
using namespace std;

int main()
{
   int x;
   float y;
   cin>>x>>y;
   if(x%5==0 && x<=y){
       cout<<fixed<<setprecision(2)<<y-(x+0.5) <<endl;
   }
   if(x%5!=0 || x>y){
       cout<<fixed<<setprecision(2)<<y<<endl;
   }
   return 0;
}
